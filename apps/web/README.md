# apps/web

Next.js + Tailwind frontend. Consumes `packages/ui` for the design tokens and
components derived from `design/ui-ux-design-system.html` (Phase 0).

Not scaffolded yet — bootstrap with `pnpm create next-app` when Phase 1 UI
work starts, then wire it into this workspace.
