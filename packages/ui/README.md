# packages/ui

Shared design tokens and components, ported from
`design/ui-ux-design-system.html` (Phase 0) into real React components that
`apps/web` imports. Includes the color/type/spacing tokens, the icon set,
rarity chips, nameplates, avatar frames, and the TCG-style product card.

Not scaffolded yet — extract from the Phase 0 HTML/CSS when Phase 1 UI work
starts.
