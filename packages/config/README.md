# packages/config

Shared `tsconfig.json`, ESLint, and Prettier config consumed by every app and
package in the workspace, so lint/format rules stay identical across
`apps/web`, `apps/api`, and everything under `packages/`.
