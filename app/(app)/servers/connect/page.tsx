import { ServerConnectWizard } from "@/features/servers/components/server-connect-wizard";

export const metadata = { title: "Connect server" };

export default function ServerConnectPage() {
  return <ServerConnectWizard />;
}
