{
  "name": "koba",
  "private": true,
  "version": "0.0.0",
  "description": "Infinite-scrolling social marketplace for game servers.",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
